# Environment-specific settings are in application-{dev,prod,test}.yaml.
telcox:
  payment:
    idempotency:
      ttl: ${PAYMENT_IDEMPOTENCY_TTL:PT24H}
    retry:
      batch-size: 100
      poll-delay: 60000
