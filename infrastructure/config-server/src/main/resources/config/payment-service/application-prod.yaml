telcox.profile: prod
server.port: 9008
spring.datasource:
  url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}
  username: ${DB_USER}
  password: ${DB_PASSWORD}
spring.jpa.properties.hibernate.format_sql: false
telcox.payment.psp.mock-mode: false
